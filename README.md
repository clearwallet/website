# Honox static website

Honoxで構築したシンプルな3ページ構成の静的サイトです。トップページ、利用規約、プライバシーポリシーを含み、SSGで`docs/`ディレクトリに出力します。

## セットアップとビルド

```bash
npm install
npm run build
```

ビルド後、`docs/` 以下に生成されたHTMLをそのままホスティングに利用できます。
